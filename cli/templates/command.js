const argsText = "%argsText%";
const description = "%description%";
const permissions = [%permissions%];

function handler(sender, channel, args, message, globals) {
    // Write your code here!
}

module.exports = {
    argsText,
    description,
    permissions,
    handler
};